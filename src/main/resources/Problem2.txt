function p2() : void {
	let a: int, b: int;
	console.read(a);
	console.read(b);

	while ( a != b) {
		if ( a > b ) {
			a = a - b;
		} else {
		b = b - a;
		}
	}
	console.print(a + " is the hcf of " + a + " and " + b);
}